import React from 'react';
import { CinematicHeroStage } from './CinematicHeroStage';

export const HeroContainer: React.FC = () => {
  return <CinematicHeroStage />;
};

export default HeroContainer;
